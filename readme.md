# Food Order Backed (Microservice Architecture)

```cmd
docker-compose build && docker-compose up
```

## Make .rar file of whole project and upload on AWS (Needs credit-card ;-;)

## Mongo URI = 'mongodb://mongo:27017/'
